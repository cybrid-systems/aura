# Issue #2057 / #2152: side-effect prim names exempt from the static coverage gate.
# Prefer fixing the registration (add_mutate / require_effect) over allowlisting.
# Format: one fully-qualified primitive name per line, with required reason:
#   name  # SECURITY_EXEMPT: <reason>
#
# mutate:set-agent-fingerprint LEFT this allowlist in #4037: the author
# fingerprint is the blame label of the next typed atomic batch, so the
# non-zero store is a TenantAdmin identity write enforced in body.
# Diagnostic / metrics-only "mutate:" names (no AST write) — security_exempt.
mutate:validate-reflected  # SECURITY_EXEMPT: diagnostic reflect counters only
# #2628: mutate:validate-against-schema demoted (mutate :validate / stats_impl)
