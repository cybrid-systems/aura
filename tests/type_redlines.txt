# Type Annotation Redlines — L6.2 TypeAnnotationNode + L6.3 TypeChecker Pass
#
# These tests go through the ABF pipeline (Racket → ABF → C++):
#   printf 'EXPR' | racket -l aura -- --abf 2>/dev/null | ./aura --abf
# Expected behavior: type annotations are stripped, inner expression evaluated.
# Type checking errors come later with the TypeChecker Pass.
#
# ── Phase 1: TypeAnnotationNode basics ──
# (define x 42) → x = 42, (: x Int) → x = 42 (annotation stripped)
# Test via E2E ABF pipeline
#
# ── Phase 2: TypeChecker basic types ──
# x : Int, x = 42 → 42 (no error)
# x : String, x = 42 → TypeError (expected String, got Int)
# x : Int, x = "hello" → TypeError (expected Int, got String)
#
# ── Phase 3: Lambda annotations ──
# (lambda ([x : Int]) x) → ok
# (lambda ([x : Int]) "hello") → TypeError (return type mismatch)
#
# ── ABF serialization ──
# TAG-TYPE-ANNOTATION (0x0F) → varint-prefixed type name + inner node
# TYPE-ANNOTATION phase 0 → entire serialized tree includes type wrapper
