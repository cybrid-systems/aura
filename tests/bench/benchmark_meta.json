{
  "schema": 1936,
  "issue": 1936,
  "description": "Per-benchmark SLO metadata for relative/statistical gate",
  "defaults": {
    "tolerance_percent": 20,
    "min_delta_ms": 20,
    "catastrophic_ratio": 3.0,
    "owner": "compiler"
  },
  "cases": {
    "literal_int": {
      "tolerance_percent": 25,
      "description": "Cold-start sensitive; allow more relative noise",
      "owner": "runtime"
    },
    "add": {
      "description": "Micro eval path",
      "owner": "evaluator"
    },
    "query_pattern_simple": {
      "min_delta_ms": 40,
      "tolerance_percent": 30,
      "description": "query:pattern after set-code; noisy under full-suite load (spikes 7→25ms)",
      "owner": "query"
    },
    "query_pattern_with_let": {
      "min_delta_ms": 40,
      "tolerance_percent": 30,
      "description": "query:pattern sibling; same suite-load variance as query_pattern_simple",
      "owner": "query"
    },
    "multi_agent_pipeline": {
      "tolerance_percent": 180,
      "catastrophic_ratio": 4.0,
      "min_delta_ms": 100,
      "description": "require std/orchestrator + fiber:spawn/join; CI co-schedule often 2–2.5× (all orch cases share ~require cost)",
      "owner": "fiber"
    },
    "par_orch_3_agents": {
      "tolerance_percent": 180,
      "catastrophic_ratio": 4.0,
      "min_delta_ms": 100,
      "description": "orch:parallel 3-way; same require/fiber CI noise as multi_agent_pipeline",
      "owner": "fiber"
    },
    "par_orch_5_agents": {
      "tolerance_percent": 180,
      "catastrophic_ratio": 4.0,
      "min_delta_ms": 100,
      "description": "orch:parallel 5-way; same require/fiber CI noise as multi_agent_pipeline",
      "owner": "fiber"
    },
    "fib_20": {
      "tolerance_percent": 40,
      "min_delta_ms": 50,
      "description": "letrec fib(20) eval; CI runner drift since 2026-07-18 baseline (1.28x / d38ms, identical pre/post #3294) - same-run literal_int 5x faster proves env change, not product regression",
      "owner": "compiler"
    }
  }
}
