;; task: valid-parens
;; goal: Write (valid-parens? s) that checks if parentheses [] {} () are balanced, test with "({[]})"
;; expect: #t
;; hint: Use (string->list s) to convert string to char codes
;; hint: Iterate through chars with (let loop ((cs chars) (stack ())) ...)
;; hint: Opening brackets (40=() 91=[ 123={) push onto stack
;; hint: Closing brackets ) ] } pop from stack and check matching
;; hint: At end: stack must be empty
;; hint: (display (valid-parens? "({[]})"))
