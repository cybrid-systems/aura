;; task: prime-test
;; goal: Write prime? that checks if n is prime, test with 17
;; expect: #t
;; hint: --- WORKING Aura prime check (COPY THIS) ---
;; hint:     (if (>= (* i i) n) #t
;; hint:       (if (= (modulo n i) 0) #f
;; hint:         (loop (+ i 1))))))
;; hint: (display (prime? 17))
;; hint: --- RULES ---
;; hint: Every (if COND THEN ELSE) needs exactly 3 sub-forms.
