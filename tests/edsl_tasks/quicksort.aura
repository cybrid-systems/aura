;; task: quicksort
;; goal: Write (quicksort lst) using std/list, test (quicksort (list 3 7 4 9 5 2 6 1))
;; depend: std/list
;; expect: 1 2 3 4 5 6 7 9
;; expect: (1 2 3 4 5 6 7 9)
;; hint: --- WORKING Aura quicksort ---
;; hint: (require std/list all:)
;; hint: (define (quicksort lst)
;; hint:   (if (< (length lst) 2) lst
;; hint:     (let ((pivot (car lst))
;; hint:           (rest (cdr lst)))
;; hint:       (append (quicksort (filter (lambda (x) (< x pivot)) rest))
;; hint:               (list pivot)
;; hint:               (quicksort (filter (lambda (x) (> x pivot)) rest))))))
;; hint: (display (quicksort (list 3 7 4 9 5 2 6 1)))
;; hint: --- NOTE ---
;; hint: length, filter, append are in std/list
