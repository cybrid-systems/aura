;; task: majority-element
;; goal: Write (majority lst) using hash that returns element appearing > n/2 times, test with (list 3 2 3)
;; expect: 3
;; depend: std/hash
;; depend: std/list
;; hint: --- WORKING Aura majority element ---
;; hint: (require std/hash all:)
;; hint:       (if (null? rest) ()
;; hint:         (let* ((n (car rest)) (c (+ 1 (if (hash-has-key? counts n) (hash-ref counts n) 0))))
;; hint:           (hash-set! counts n c)
;; hint:           (if (> c half) n (loop (cdr rest) half)))))))
;; hint: (display (majority (list 3 2 3)))
;; hint: --- NOTE ---
;; hint: Use (require std/list all:) for length
