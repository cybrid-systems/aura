;; task: string-reverse
;; goal: Write (str-rev s) that reverses a string using list->string and string->list
;; expect: !dlroW olleH
;; depend: std/list
;; hint: --- WORKING Aura string reverse ---
;; hint: (define (str-rev s)
;; hint:   (list->string (reverse (string->list s))))
;; hint: (display (str-rev "Hello World!"))
;; hint: --- NOTE ---
;; hint: reverse is in std/list, load with (require std/list all:)
