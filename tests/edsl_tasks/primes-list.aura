;; task: primes-list
;; goal: Write (primes n) returning all primes <= n using std/list filter
;; expect: 2
;; expect: 3
;; expect: 5
;; expect: 7
;; depend: std/list
;; hint: --- WORKING Aura primes list (COPY THIS) ---
;; hint: (require std/list all:)
;; hint: (define (prime? n)
;; hint:   (let loop ((i 2))
;; hint:     (if (>= (* i i) n) #t
;; hint:       (if (= (modulo n i) 0) #f
;; hint:         (loop (+ i 1))))))
;; hint: (define (primes n)
;; hint:   (filter (lambda (x) (prime? x)) (range 2 n)))
;; hint: (display (primes 20))
