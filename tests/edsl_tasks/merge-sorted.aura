;; task: merge-sorted
;; goal: Write (merge a b) that merges two sorted lists into one sorted list, test with (list 1 3 5) and (list 2 4 6)
;; expect: 1 2 3 4 5 6
;; depend: std/list
;; hint: --- WORKING Aura merge sorted ---
;; hint: (require std/list all:)
;; hint: (define (merge a b)
;; hint:   (cond ((null? a) b)
;; hint:         ((null? b) a)
;; hint:         ((< (car a) (car b)) (cons (car a) (merge (cdr a) b)))
;; hint:         (#t (cons (car b) (merge a (cdr b))))))
;; hint: (display (merge (list 1 3 5) (list 2 4 6)))
