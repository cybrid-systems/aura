;; task: tcp-connect
;; goal: Connect to httpbin.org:80, send HTTP GET, receive response
;; expect: 200
;; expect: OK
;; hint: Use (tcp-connect "httpbin.org" 80) to get a socket
;; hint: Send HTTP GET with (tcp-send socket "GET / HTTP/1.0\r\nHost: httpbin.org\r\n\r\n")
;; hint: Receive response with (tcp-recv socket)
;; hint: HTTP response starts with "HTTP/1.1 200 OK" — extract the status code and message
;; hint: Use (display ...) to show the socket operations result
