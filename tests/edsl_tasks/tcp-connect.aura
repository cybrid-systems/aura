;; task: tcp-connect
;; goal: Connect to httpbin.org:80, send HTTP GET, receive response
;; expect: 200
;; expect: OK
;; hint: TCP flow: connect → send → receive → display result
;; hint: (tcp-connect "httpbin.org" 80) → socket fd (integer)
;; hint: (tcp-send fd "GET / HTTP/1.0\r\nHost: httpbin.org\r\n\r\n")
;; hint: (tcp-recv fd) returns HTTP response string (head + body)
;; hint: HTTP response starts with "HTTP/1.1 200 OK" — display the full response
;; hint: Use (display (tcp-recv socket)) to show the raw result
