;; task: tcp-connect
;; goal: Connect to httpbin.org:80, send HTTP GET, receive response
;; expect: 200
;; expect: OK
;; hint: --- TCP socket ---
;; hint: Aura has (tcp-connect "host" port). DO NOT use c-func for networking.
;; hint: (define s (tcp-connect "httpbin.org" 80))
;; hint: (display "ok")
;; hint: }
;; hint: 
;; hint: # ── LLM 调用 ──────────────────────────────────────────────
;; hint: def llm_complete(model, base_url, key, messages, retries=3):
;; hint:     parsed = urllib.parse.urlparse(base_url)
;; hint:     path = parsed.path.rstrip("/") + "/chat/completions"
;; hint:     for attempt in range(retries):
;; hint:         try:
;; hint:             conn_cls = http.client.HTTPSConnection if parsed.scheme == "https" else http.client.HTTPConnection
;; hint:             h = conn_cls(parsed.netloc, timeout=120)
;; hint:             h.request("POST", path, json.dumps({
