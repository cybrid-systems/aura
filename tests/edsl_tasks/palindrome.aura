;; task: palindrome
;; goal: Write (palindrome? s) that checks if a string reads same forward/backward, test with "racecar"
;; expect: #t
;; depend: std/list
;; hint: --- WORKING Aura palindrome check ---
;; hint: (require std/list all:)
;; hint: (define (palindrome? s)
;; hint:   (let ((chars (string->list s)))
;; hint:     (= chars (reverse chars))))
;; hint: (display (palindrome? "racecar"))
