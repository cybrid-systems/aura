;; task: list-partition
;; goal: Write (partition pred lst) that splits a list into (passed failed) using std/list filter
;; expect: 1 3 5
;; expect: 2 4
;; expect: ((1 3 5) (2 4))
;; depend: std/list
;; hint: --- WORKING Aura partition ---
;; hint: (require std/list all:)
;; hint: (define (odd? n) (= (modulo n 2) 1))
;; hint: (define (partition pred lst)
;; hint:   (list (filter pred lst)
;; hint:         (filter (lambda (x) (not (pred x))) lst)))
;; hint: (display (partition odd? (list 1 2 3 4 5)))
