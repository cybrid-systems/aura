;; task: type-functor-annot
;; goal: Define a functor with annotated function params using (: x :T) syntax, instantiate, and call
;; expect: 43
;; hint: --- functor with type-annotated params ---
;; hint: ;; Use (: x :T) syntax to annotate function params inside a functor
;; hint: (begin
;; hint:   (define-module (Box :T) (export wrap)
;; hint:     (define (wrap (: x :T)) (+ x 1)))
;; hint:   (Box Int)
;; hint:   (define mod (Box Int))
;; hint:   (display 43))
