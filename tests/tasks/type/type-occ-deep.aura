;; task: type-occ-deep
;; goal: Deeply nested occurrence typing — multiple levels of if with type predicates
;; expect: 6
(let ((x "hello"))
  (if (string? x)
    (if (number? (string-length x))
      (string-length x)
      0)
    0))
(newline)
