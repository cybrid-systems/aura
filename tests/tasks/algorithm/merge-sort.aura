;; task: merge-sort
;; goal: Write merge sort using std/list
;; expect: 1
;; expect: 2
;; expect: 3
;; expect: 4
;; expect: 5
;; depend: std/list
;; hint: --- WORKING Aura merge sort ---
;; hint: (require std/list all:)
;; hint:   (cond
;; hint:     ((null? a) b)
;; hint:     ((null? b) a)
;; hint:     ((< (car a) (car b)) (cons (car a) (merge (cdr a) b)))
;; hint:     (else (cons (car b) (merge a (cdr b))))))
;; hint:   (if (or (null? lst) (null? (cdr lst))) lst
;; hint:       (merge (merge-sort (take mid lst)) (merge-sort (drop mid lst))))))
;; hint: (display (merge-sort (list 3 1 4 1 5 9 2 6)))
