;; task: table-lookup
;; goal: Write (lookup key table) that searches a list of pairs by key using std/list filter
;; expect: 30
;; depend: std/list
;; hint: --- WORKING Aura table lookup ---
;; hint: (require std/list all:)
;; hint:     (if (null? matches) ()
;; hint:       (car (cdr (car matches))))))
;; hint: (display (lookup "c" table))
