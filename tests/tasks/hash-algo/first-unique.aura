;; task: first-unique
;; goal: Write (first-unique s) that returns first non-repeating character in string, test with "leetcode"
;; expect: 108
;; hint: --- WORKING Aura first unique char ---
;; hint: (require std/hash all:)
;; hint:     (for-each (lambda (c) (hash-set! counts c (+ 1 (if (hash-has-key? counts c) (hash-ref counts c) 0)))) chars)
;; hint:       (if (null? cs) -1
;; hint:         (if (= (hash-ref counts (car cs)) 1) (car cs) (loop (cdr cs)))))))
;; hint: (display (first-unique "leetcode"))
