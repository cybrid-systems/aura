;; task: max-subarray
;; goal: Write (max-subarray lst) using Kadane's algorithm that finds max contiguous sum, test with (list -2 1 -3 4 -1 2 1 -5 4)
;; expect: 6
;; depend: std/list
;; hint: --- WORKING Aura max subarray ---
;; hint: (require std/list all:)
;; hint:     (if (null? rest) best
;; hint:       (let* ((n (car rest)) (new-cur (max n (+ cur n))))
;; hint:         (loop (cdr rest) new-cur (max best new-cur))))))
;; hint: (display (max-subarray (list -2 1 -3 4 -1 2 1 -5 4)))
