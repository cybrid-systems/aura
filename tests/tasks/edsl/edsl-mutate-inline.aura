;; task: edsl-mutate-inline
;; goal: Use mutate:inline-call to inline a function call
;; expect: 6
;; hint: --- mutate:inline-call ---
;; hint: ;; Step 1: Load code with a wrapper function
;; hint: (set-code "(begin (define (f x) (* x 3)) (define (g x) (f x)))")
;; hint: ;; Step 2: Find the (f x) call in g and inline it
;; hint: (eval-current)
;; hint: (define def-g (car (cdr (query:children (query:root)))))
;; hint: (define lam-g (car (query:children def-g)))
;; hint: (define body (car (query:children lam-g)))
;; hint: (mutate:inline-call body)
;; hint: ;; Step 3: Verify g still works
;; hint: (eval-current)
;; hint: (display (g 2))
