;; task: edsl-workspace
;; goal: Use workspace:create, switch, and merge to isolate changes
;; expect: ws
;; hint: --- Workspace layering ---
;; hint: (set-code "(define (g x) (+ x 1))")
;; hint: (define ws (workspace:create "sandbox"))
;; hint: (workspace:switch ws)
;; hint: (mutate:rebind "g" "(lambda (x) (* x 2))" "mutate")
;; hint: (workspace:switch 0)
;; hint: (workspace:merge ws)
;; hint: (display (car (query:def-use "g")))

