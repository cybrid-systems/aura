;; task: edsl-synthesize-pipeline
;; goal: Use synthesize:pipeline to chain multiple steps
;; expect: Complete
;; hint: --- Pipeline strategy ---
;; hint: (require "std/pipeline" all:)
;; hint: (set-code "(begin)")
;; hint: (synthesize:register-template "add"
;; hint:   "(define (add-{name} x y) (+ x y))" "name")
;; hint: (synthesize:pipeline "test"
;; hint:   (synthesize:fill "add" "nums"))
;; hint: (display "Pipeline complete")

