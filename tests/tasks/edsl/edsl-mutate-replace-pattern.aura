;; task: edsl-mutate-replace-pattern
;; goal: Use mutate:replace-pattern to transform code
;; expect: 10
;; hint: --- mutate:replace-pattern ---
;; hint: ;; Step 1: Load code with a pattern to replace
;; hint: (set-code "(define (f x) (* x 2))")
;; hint: ;; Step 2: Replace (* x 2) with (+ x x)
;; hint: (eval-current)
;; hint: (mutate:replace-pattern "(* x 2)" "(+ x x)")
;; hint: ;; Step 3: Verify f still works (same semantics for x=5)
;; hint: (eval-current)
;; hint: (display (f 5))
