;; task: edsl-mutate-chain
;; goal: Use set-code + mutate:rebind to change a function's behavior, then eval-current
;; expect: 42
;; depend: edsl
;; hint: --- EDSL set-code + mutate:rebind + eval-current ---
;; hint: ;; Step 1: Load a function
;; hint: (set-code "(define (f) (* 6 7))")
;; hint: ;; Step 2: Rebind to a simpler body
;; hint: (mutate:rebind "f" "(lambda () 42)")
;; hint: ;; Step 3: Evaluate the current workspace
;; hint: (display (f))
