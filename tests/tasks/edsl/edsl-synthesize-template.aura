;; task: edsl-synthesize-template
;; goal: Use synthesize:register-template and synthesize:fill
;; expect: handle
;; hint: --- Template strategy ---
;; hint: (set-code "(begin)")
;; hint: (synthesize:register-template "handler"
;; hint:   "(define (handle-{name} req) (display req))" "name")
;; hint: (synthesize:fill "handler" "test")
;; hint: (display (current-source))

