;; task: edsl-colony
;; goal: Use colony:search to find a working local mutation when output is wrong
;; expect: 42
;; depend: ant
;; hint: --- EDSL colony search ---
;; hint: ;; Use set-code + colony:search to find a fix
;; hint: (set-code "(define (foo) (* 6 7))")
;; hint: ;; colony:search tries local mutations automatically
;; hint: (colony:search "foo" "(lambda () (* 6 7))")
;; hint: (display (foo))
