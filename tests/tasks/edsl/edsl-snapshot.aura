;; task: edsl-snapshot
;; goal: Use ast:snapshot then mutate then ast:restore to revert changes
;; expect: g
;; hint: --- EDSL snapshot/restore ---
;; hint: (set-code "(define (g x) (* x 2))")
;; hint: (define sid (ast:snapshot "before"))
;; hint: (mutate:rebind "g" "(lambda (x) (+ x 1))" "test")
;; hint: (ast:restore sid)
;; hint: (display (car (query:def-use "g")))

