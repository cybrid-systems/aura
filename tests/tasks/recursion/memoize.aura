;; task: memoize
;; goal: Write (memoize fn) that wraps a function with a cache using hash, test with fibonacci
;; expect: 55
;; depend: std/hash
;; hint: --- WORKING Aura memoize ---
;; hint: (require std/hash all:)
;; hint:       (if (hash-has-key? cache x)
;; hint:         (hash-ref cache x)
;; hint:           (hash-set! cache x r)
;; hint:           r))))))
;; hint:   (if (< n 2) n (+ (fib (- n 1)) (fib (- n 2)))))
;; hint: (display (memo-fib 10))
