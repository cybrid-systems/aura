(define (vec:range start end)
  (let* ((n (- end start))
         (v (make-vector n 0)))
    (let loop ((i 0))
      (if (< i n)
        (begin (vector-set! v i (+ start i))
               (loop (+ i 1)))
        v))))
