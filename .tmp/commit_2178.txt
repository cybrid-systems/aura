feat(hot-update): explicit cross-workspace / cross-COW hot-update reject + metric (#2178, refine #1943 #2012)

Make the documented single-workspace boundary (#1943) enforceable
and observable. Foreign eval contexts (or COW generation divergence)
are now rejected at the aura_reload_aot_module_for_eval entry point
with a dedicated metric + audit, never silent partial success.
The MVP scope is enforced until a future cross-COW migration design
lands — the observable guard means silent partial success is no
longer possible in the multi-agent / multi-tenant host case.

5 files / 339 insertions:

- src/compiler/aura_jit_bridge.cpp: file-level atomic
  g_cross_workspace_hot_update_rejected_total + C-linkage
  accessor + record helper + forward decl + definition of
  aura_is_current_workspace_eval (uses the existing g_aot_state_mtx
  + g_aot_state_map for the lookup). Foreign-eval guard inserted at
  the top of aura_reload_aot_module_for_eval — bumps the counter,
  sets last-fail reason + audit, returns false. Null or matching
  eval unchanged (happy path).

- src/compiler/observability_metrics.h: CompilerMetrics field
  cross_workspace_hot_update_rejected_total (next to the other
  aot_reload_*_total counters for the Agent dashboard mirror).

- src/compiler/hot_update_registry.hh: contract doc next to the
  MVP comment — links the guard + counter to the single-workspace
  boundary enforcement.

- tests/compiler/test_aot_reload_primitive.cpp: header comment
  updated + ac7_cross_workspace_reject_2178 (foreign → reject + null
  → happy + source-cite for all production-side symbols). Forward
  decls added for the C-linkage accessors.

- scripts/check_cross_workspace_hot_update_reject_coverage.py:
  CI-gate linter with 5 contract rows (aura_jit_bridge atomics,
  observability_metrics field, guard pattern in
  aura_reload_aot_module_for_eval, hot_update_registry.hh doc,
  test_aot_reload_primitive.cpp AC7). --self-test OK.

AC7: source-cite checks pass + live value check (counter is atomic
load). Pre-existing test_issue_* link errors (aura_bump_live_closure_stable_id_backfill_total
undefined + duplicate symbol: main in tests/bundles) are unrelated
to this change and are skipped by the build script.

Refines #1943 (single-workspace MVP) + #2012 (version-keyed swap).
Non-duplicative: existing note_macro_introduction_total / version
mismatch counters are unchanged; this adds a new cross-workspace
guard + counter as documented in the MVP contract.

Token lesson applied (per #2170 close): Python urllib for close.
Pipeline: build (✓ build OK at main level; pre-existing test_issue_*
errors are skipped) -> linter self-test OK -> pre-push gate ->
push -> close.