# 2026-07-14

#1485 ship in progress — C1 implemented and committed locally:

- `src/compiler/observability_metrics.h`: added 2 new atomics on
  `CompilerMetrics` (`stale_closure_prevented`, `closure_epoch_mismatch_fallback`).
- `src/compiler/evaluator.ixx`: 4 Evaluator accessor / bump helpers wired
  to the new atomics through `compiler_metrics_` pointer.
- `src/compiler/evaluator_eval_flat.cpp`: wire in `closure_needs_safe_fallback`
  (epoch_stale local) + inline race-window bump in `apply_closure` dual-path
  for flat*/pool* dangling case.
- `src/compiler/aura_jit_bridge.{h,cpp,stub.cpp}`:
  `aura_set_current_bridge_epoch` decl + impl + weak stub (foundation
  for the C2-wire fix-up).

Refines #1475 (apply_closure epoch check). Follow-on from #1508
(dual-provenance).
