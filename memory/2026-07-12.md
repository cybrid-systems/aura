# 2026-07-12

#1485 audit + 5-phase ship plan finalized.

## Cross-references

- #1475 (apply_closure epoch check) — the surface this defense layers on.
- #1508 (dual-provenance) — bridge_epoch + defuse_version_ pairing.
- #1476 (dual-epoch protocol) — acq/rel memory ordering pattern reused in
  the C2-wire fix-up (service.ixx::bump_bridge_epoch).

## Audit plan (5 commits, scope-limited close)

1. **C1** — `stale_closure_prevented` + `closure_epoch_mismatch_fallback`
   2 new atomics on `CompilerMetrics` + 4 Evaluator accessor / bump
   helpers + wire in `closure_needs_safe_fallback` helper (epoch_stale
   local) + inline race-window bump in `apply_closure` dual-path.
2. **C2** — per-closure provenance accessors
   (`aura_get_closure_bridge_epoch`, `aura_get_closure_defuse_version`)
   for JIT emit-side freshness probe. Reads under `shared_lock` on
   `g_closure_table_mtx`. Out-of-range returns 0.
3. **C2-wire (separate fix-up, **not** in original audit plan)** —
   `service.ixx::bump_bridge_epoch` also calls
   `aura_set_current_bridge_epoch(...)` so `lib/runtime.c`'s
   `aura_closure_call` 2-check sees the fresh value.
4. **C3** — `tests/test_issue_1485.cpp` + `CMakeLists.txt`
   `aura_add_issue_test(test_issue_1485)` + `tests/test-binding-allowlist.txt`
   + `docs/generated/test-registry.json` chore. Link may hit system 5-min
   build timeout per invariant #29; verify deferred to follow-up #1538.
5. **C4** — `git push origin main` + PATCH issue close + POST comment +
   update MEMORY.md + 5 memory chore commits.

## Why scope-limited

The full audit plan shipped end-to-end. Followups (out of scope for
#1485, tracked separately):
- LLVM IR-level CreateCall emit-side probe (the extern accessors let emit
  wire one when needed; the wire itself is deferred — requires basic-block
  splitting in `aura_jit.cpp`'s OpApply / OpCall cases, non-trivial LLVM
  pattern change).
- Aura primitive exposure for the 2 new stats counters (same primitive
  pattern as `query:incremental-relower-stats` from #1483).
