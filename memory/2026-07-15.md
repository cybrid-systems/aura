# 2026-07-15

#1485 ship continues — C2 + C2-wire fix-up committed locally:

- `src/compiler/aura_jit_bridge.h`: added extern decls for
  `aura_get_closure_bridge_epoch` + `aura_get_closure_defuse_version`.
- `src/compiler/aura_jit_runtime.cpp`: impls under `shared_lock` on
  `g_closure_table_mtx` (concurrent alloc/free resize under unique_lock
  don't race). Out-of-range returns 0.
- `src/compiler/aura_jit_bridge_stub.cpp`: weak stubs (return 0) for test
  binaries that don't link the runtime. Weak so production impl wins.

**C2-wire fix-up** — caught during stage of C1 commit: `aura_set_current_bridge_epoch`
extern decl + impl + weak stub had been bundled into the C1 foundation
phase, but the actual call site (`service.ixx::bump_bridge_epoch`) was
uncommitted. Without it the C-side `g_current_bridge_epoch` stays at the
static-init default (0) and `lib/runtime.c`'s `aura_closure_call` 2-check
passes vacuously. Fix-up:

- `src/compiler/service.ixx::bump_bridge_epoch` — after the
  `mutation_epoch_.fetch_add(1, std::memory_order_release)` call, also
  call `aura_set_current_bridge_epoch(mutation_epoch_.load(std::memory_order_relaxed))`.

Acq/rel pairing with the fetch_add mirrors the existing protocol
documented in `service.ixx` (acquire-load counters in `is_bridge_stale`,
see #1475 / #1476).
