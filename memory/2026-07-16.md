# 2026-07-16

#1485 ship — final integration prep + lib/runtime.c format drift fix.

`lib/runtime.c` clang-format drift committed (the AuraPair struct reformat
+ IS_PAIR / FLOAT_BIAS macro whitespace cleanup landed via `clang-format
-i` during the C1 staging round but never got committed — oversight during
`git` staging; final `add` didn't include it). Diff is whitespace + struct
field alignment only (no semantic change, doesn't affect runtime behavior).

## Pre-push gate hygiene

Per #1453 / #1478 / #1480 precedent for the #1485 C3 test that goes
through `tests/test-binding-allowlist.txt`:

- Verify link timeout per invariant #29 → deferred to #1538 batch.
- aot_incremental_reemit_count / aot_closure_dependency_reemit_total /
  aot_region_filtered_skips / aot_closure_bridge_refresh_total last-call
  stats (the close peers from #1480) stable.
- ClosureBridge epoch advance pattern unchanged.

## Ship plan for next-day

- C3: write `tests/test_issue_1485.cpp` (10 ACs covering C1 metrics + C2
  accessors + C2-wire bridge roundtrip), register in `CMakeLists.txt`,
  add to `tests/test-binding-allowlist.txt`, refresh
  `docs/generated/test-registry.json` (count + integration count).
- C4: `git push origin main` + PATCH `/repos/cybrid-systems/aura/issues/1485`
  (close + state_reason=completed) + POST comment + update MEMORY.md +
  5 memory chore commits.
