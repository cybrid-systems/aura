; Aura module loader — require
;
; require 现在是一个内建特殊形式，无需 import 即可使用。
; 此文件保留为空以便向后兼容。
;
; 用法:
;   (require std/list)    — 符号形式
;   (require "std/list")  — 字符串形式
;
; require 是内建语法，和 import 的区别:
;   import: 参数必须是字符串字面量 "(import \"std/list\")"
;   require: 参数可以是符号或字符串 (require std/list) 或 (require "std/list")

; 空文件 — require 已在 C++ 求值器中作为特殊形式实现
